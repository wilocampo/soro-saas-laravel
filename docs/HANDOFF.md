# HANDOFF — Accounting SaaS (working title)

> Purpose: single source of truth for any Claude Code session (or human) picking up this project.
> Location: keep this at `docs/HANDOFF.md`. Pair it with `CLAUDE.md` at repo root and the specs in `docs/specs/`.
> Update this file at the end of every working session. Stale handoffs are worse than none.

## 0. Read this first — spec package index

The decisions below are grounded in a repo inventory and a fact-checked BIR research pass done 2026-07-20/21. The detailed, implementable specs live here:

- [`docs/specs/01-ledger-data-model.md`](specs/01-ledger-data-model.md) — MariaDB 11.8 schema + integrity-enforcement stack.
- [`docs/specs/02-posting-engine.md`](specs/02-posting-engine.md) — `PostingService`, posting rules, accrual/cash, rounding, reversal, worked examples.
- [`docs/specs/03-bir-accreditation.md`](specs/03-bir-accreditation.md) — **the BIR/CAS compliance reference** (citation-backed, adversarially fact-checked). Read before touching tax, invoicing, books, or audit trail.
- [`docs/specs/04-roles-permissions.md`](specs/04-roles-permissions.md) — permission matrix + CAS security rules.
- [`docs/specs/05-testing-strategy.md`](specs/05-testing-strategy.md) — the ledger test gate.
- [`docs/specs/06-decisions.md`](specs/06-decisions.md) — ADRs + **accountant sign-off list** + open build decisions.
- [`docs/specs/07-glossary.md`](specs/07-glossary.md) — double-entry + PH/BIR vocabulary.
- [`docs/specs/08-inventory-module.md`](specs/08-inventory-module.md) — **inventory** (in scope as of 2026-07-21): weighted-average perpetual COGS, multi-location, lots/FEFO, counts & variance, easy receiving UX.
- [`docs/specs/09-implementation-kit.md`](specs/09-implementation-kit.md) — verified package install list, frontend components, `package.json` fixes, PHP-floor decision.
- [`docs/specs/10-operations.md`](specs/10-operations.md) — per-tenant backups (⛔ current config misses tenant DBs), fleet migrations, CI shape, **Data Privacy Act (RA 10173)** obligations, security ops, RPO/RTO, timezone.
- [`docs/specs/11-frontend-components.md`](specs/11-frontend-components.md) — **Sakai design system + reusable component library**: audit of non-Sakai pages (Profile/Settings/Tenants/Notifications/auth), the full build list (tables, form fields, money/TIN inputs, confirm/toast patterns, accounting grids), and the conversion backlog.
- [`docs/fixtures/`](fixtures/) — hand-verifiable posting scenarios (draft for accountant review), incl. month-close + inventory round-trip.

## 1. Product decision (read first)

- **Not** a full QuickBooks Online clone. Scope = double-entry books + invoicing + expenses + **inventory (weighted-average perpetual COGS, variance detection — spec `08`)** + reports for Philippine SMEs, with **BIR compliance as the differentiator**. **Both VAT and NON-VAT taxpayers are first-class** (most PH SMEs are non-VAT: percentage-tax posting rules, non-VAT invoice variant, 2551Q).
- Target reports v1: Trial Balance, Income Statement (P&L), Balance Sheet, General Ledger, General Journal. BIR-oriented outputs (columnar books of accounts, VAT summary, 2307/withholding, SLSP/alphalist `.dat`) are the moat — spec'd in `03-bir-accreditation.md`; **require accountant/CPA review before shipping.**
- Deployment: one codebase, two modes via config:
  - `SINGLE_TENANT=true` → registration disabled, seeded with one tenant (per-client VPS install)
  - default → open SaaS with tenant signup + billing (`laravel/cashier` already installed)

## 2. Architecture decisions (locked — corrected against the actual codebase)

> These were re-derived from a real repo inventory. The earlier version of this file stated several things the code contradicts; those are corrected here.

- **Framework:** **Laravel 12** (PHP 8.2+) — the repo is on `laravel/framework: ^12.0`. There is **no L13 upgrade task**; build on 12. **MariaDB 11.8** (`mariadb` driver; ≥ 10.5 floor for enforced CHECKs — verified on dev 2026-07-21; install MariaDB on target VPS).
- **Tenancy:** **database-per-tenant** via `spatie/laravel-multitenancy` (subdomain finder + connection-swap in `app/Multitenancy/SwitchTenantDatabaseTask.php`). Physical isolation. **Ledger tables live in each tenant DB; no `tenant_id` columns on ledger tables.** The `users.tenant_id` column is vestigial shared-DB scaffolding — remove or ignore for ledger work. (This corrects the old "shared tables + tenant_id / Postgres RLS" note — RLS is unnecessary given physical isolation.)
- **Ledger-first core (non-negotiable):**
  - `journal_entries` (header) + `journal_lines` (debit/credit lines) are the source of truth.
  - Every business document (invoice, bill, payment, adjustment) POSTS to the journal via a single `PostingService`; it never IS the balance.
  - Account balances are always **derived** (sum of lines). Materialized `account_period_balances` is an **invalidatable, rebuildable cache**, never mutable truth.
  - Posted entries are **immutable**. Corrections = reversing entries. Documents are **voided, never edited/hard-deleted** (BIR rule — RMC 5-2021 Annex B item 10).
  - Period close: lock date per tenant; posting on/before the lock date, or into a closed period, is rejected.
  - Every entry must balance (Σdebits = Σcredits) — enforced at DB (per-row CHECK + trigger on the `draft→posted` flip) AND app level. See `01`.
- **Chart of accounts:** per-tenant, seeded from a PH SME default template. Types: Asset / Liability / Equity / Income / Expense (+ contra flag, parent/sub hierarchy).
- **Money:** **integer minor units (centavos, `BIGINT`)** — never floats/DECIMAL in the ledger. Single currency (PHP) for v1; multi-currency out of scope.
- **Build our own core.** Do **not** adopt a package as the ledger dependency. `ekmungai/eloquent-ifrs` (MIT) and `scottlaurent/accounting` (MIT) are **reference implementations only** (eloquent-ifrs stores money as floats and its multi-entity model is shared-DB — both wrong for us). **Do not derive from Akaunting** — its BSL 1.1 license forbids offering it as a commercial accounting service. (Resolves old open-question §6.5.) See `06-decisions.md`.

## 3. Repo status

- This repo is a **near-stock Laravel 12 skeleton** with: `spatie/laravel-multitenancy` (DB-per-tenant, subdomain), `spatie/laravel-permission` (teams OFF), Breeze auth (open registration), `laravel/cashier` (installed, unused), Inertia + Vue 3 + PrimeVue (Sakai), `nwidart/laravel-modules` (installed, no module exists), Telescope, media-library, backup. **No accounting domain exists yet.**
- **Known base-repo debt to fix in Phase 0** (see `06-decisions.md`): two divergent `tenants` migrations (`database/migrations/` vs `database/migrations/landlord/`); newly provisioned tenant DBs are **never seeded** (no roles/admin) and `DatabaseSeeder` never calls `RolePermissionSeeder`; tenant DBs are migrated with the *landlord* set (no `--path`) → need a dedicated `database/migrations/tenant/` path; `nwidart` config path (`Modules/`) ≠ composer merge path (`app/Modules/*`); no `SINGLE_TENANT` concept; Inertia client v1 vs server v2; case-variant `components/`+`Components/`, `layout/`+`Layouts/`.
- **Reusable:** DB-per-tenant isolation, subdomain routing, Breeze auth, roles/permissions, and `CRUDDataTable`/`CRUDForm`/`CRUDField`/`CRUDModal` Vue components for accounting UI.

## 4. Phase plan (exit criteria per phase)

- **Phase 0 — Foundation & cleanup:** fix the base-repo debt; dedicated tenant migration path; provisioning saga (seed roles + admin + `system` user, per-DB grants, failure compensation); `SINGLE_TENANT` mode; Money value object; **fix `package.json` conflicts (Inertia v1/v2, Tailwind v3/v4)**; **PHP `^8.3` floor decision (D13)**; **timezone → Asia/Manila (D18)**; **CI up (pint + larastan + MariaDB-11.8 Ledger suite — spec `10` §3)**; **tenant-aware queue test**; **per-tenant backup orchestration (spec `10` §1 — current config misses tenant DBs)**; verify MySQL ≥ 8.0.16. *Exit: a freshly provisioned tenant DB is fully usable; landlord/tenant schemas cleanly separated; SINGLE_TENANT works; CI green incl. Ledger suite; a tenant backup restores clean with triggers intact.*
- **Phase 1 — Ledger core:** CoA, journal entries/lines, `PostingService`, enforcement stack, gapless numbering (per-branch series), fiscal periods + lock (+ period-13 adjustment period), opening balances, trial balance, `tax_codes`/`atc_rates`/`company_profile`/`tax_regimes`/`account_roles` tables, `ledger:verify`/`ledger:rebuild-balances`, password-rotation job. *Exit: generative tests prove the trial balance always balances under random valid postings; unbalanced/edited/pre-lock/duplicate postings are rejected; audit-log hash chain verifies.*
- **Phase 2 — Documents:** customers/vendors (+ TIN/branch, sworn-declaration validity, CSV import incl. CoA + open documents), invoices, bills, payments **with allocation model (partials, over-payments, advances/deposits)**, expenses (+ receipt attachments via medialibrary), **credit/debit memos + the cancellation flow (void in-period, credit note after filing)**, **non-VAT posting rules + invoice variant**, **BIR-compliant invoice + supplementary-doc PDF templates (11 mandatory fields, "not valid for input tax" marker) + email delivery** → each posts via a `PostingRule` (accrual/cash templates); A/R & A/P aging from the subledger. *Exit: worked fixtures post correct debits/credits in both bases and both regimes; reversal unwinds documents cleanly; an issued invoice renders a compliant PDF.*
- **Phase 2b — Inventory (spec `08`):** items/UoM/barcodes/locations, receiving (+GRNI), perpetual weighted-average COGS, transfers, counts + variance workflow, lots/FEFO/expiry, `inventory:verify`. *Exit: receive→sell→count round-trip posts correct JEs; subledger ties to GL to the centavo; count variance yields an immutable approved adjustment.*
- **Phase 3 — Reports:** P&L, Balance Sheet, GL, GJ, **customer SOA, manual bank reconciliation, dashboard-lite (cash, AR/AP overdue, P&L snapshot, upcoming deadlines)**; PDF/XLSX (spatie-pdf/openspout); Statement of Cash Flows or explicit v2 deferral. *Exit: reports tie to the trial balance to the centavo; year-end close yields a correct post-closing balance sheet.*
- **Phase 4 — BIR layer (accountant-gated):** VAT (2550Q) **+ percentage tax (2551Q)**, withholding/2307 + QAP/alphalist, columnar books of accounts (incl. **Inventory Book** + annual Inventory List), EOPT invoice serial rules, SLSP/alphalist `.dat` (golden-file tested), ACCN + mandatory report header/footer, **compliance calendar with deadline reminders**, registration-pack generator; **EIS transmission: named deferral with the 31-Dec-2026 mandate risk stated, emitter interface stubbed**. **Do not ship on Claude output alone; CPA sign-off required.**
- **Phase 5 — SaaS shell:** wire the existing `cashier` billing, tenant onboarding (**ACCN gate + NPCRS/DPA registration gate** + SINGLE_TENANT), offboarding export bundle, production hardening checklist (spec `10` §7), single-tenant deploy (Docker Compose incl. Chromium for PDF).

## 5. Testing policy

- The ledger core gets the strictest tests in the codebase (PHPUnit + a generative harness). See `05-testing-strategy.md`.
- **Gate:** any PR touching `journal_*` or `PostingService` requires the full ledger suite green.

## 6. Open questions — resolved + remaining

Resolved (see `06-decisions.md`):
- Accrual **and** cash basis, as a **per-tenant setting** (not per-transaction); basis-selective posting.
- **Inventory IN scope (2026-07-21, reverses the earlier call):** moving weighted average, perpetual COGS, multi-location, lots/FEFO v1 (D9–D12, spec `08`).
- Bank feeds **v2**; manual reconciliation v1 (Phase 3). Payroll + income-tax returns explicitly out of scope (D8).
- License: build our own core; Akaunting rejected (BSL); eloquent-ifrs/scottlaurent reference-only. Packages per `09` (D21).
- Timezone Asia/Manila (D18); PHP `^8.3` floor pending VPS check (D13); TDE for encryption at rest (D19); users + `system` actor per tenant DB (D20).

Remaining (need a PH CPA/tax lawyer — see `06` and the "Open items" in `03-bir-accreditation.md`):
- Retention 5 vs 10 years (RR 7-2024 vs older issuances); VAT rounding method; exact ATC↔rate table; year-end close mechanics (Income Summary vs direct-to-RE, drawings/dividends, MCIT/NOLCO); who reviews Phase-1 fixtures and Phase-4 outputs.

## 7. Session log

- 2026-07-20 — Initial handoff created from planning discussion.
- 2026-07-21 — Repo inventory done (§3 corrected: L12, DB-per-tenant, cashier present). Full spec package produced (`docs/specs/01-07`, `CLAUDE.md`, fixtures). Build-vs-buy resolved (own core; reference eloquent-ifrs/scottlaurent; reject Akaunting). BIR/CAS compliance researched + fact-checked → `03-bir-accreditation.md`. Key corrections: retention 5-yr (conflict flagged), EOPT "Invoice" is the single principal VAT doc (OR supplementary), append-only grounded in RMC 5-2021 not RR 9-2009.
- 2026-07-21 (later 2) — Frontend audit vs Sakai (`primefaces/sakai-vue` structure verified): shell is correctly Sakai but Profile/Settings/Tenants/Notifications + 4 auth pages are Breeze/Tailwind; CRUD components carry PrimeFlex/v3 legacy classes; Confirm/Toast services registered but unused; two component roots. → `11-frontend-components.md` (conventions, component build list incl. accounting grids, conversion backlog; kit + Users/layout fixes pulled into Phase 0 exit criteria).
- 2026-07-21 (later) — Four-lens gap review (packages / product / operations / consistency) + ninetails inventory prior-art mining. **Inventory brought into scope** (D9–D12) → spec `08`. Added `09-implementation-kit.md` (verified packages; no BIR `.dat` package exists — hand-build; mpdf rejected GPL) and `10-operations.md` (per-tenant backups ⛔, fleet migrations, CI, **RA 10173/DPA obligations + NPCRS gate**, security, RPO/RTO, Asia/Manila). Fixed 4 spec blockers (missing `tax_codes`/profile tables → `01` §7; entry-number format mandate; T4 vs `reverse()`; period-13 close deadlock) + the S4 ATC/rate mismatch (now 2% WC160). Product gaps folded into phases: credit notes, payment allocations, **non-VAT/2551Q as first-class**, compliant invoice PDF in Phase 2, SOA/recon/dashboard in Phase 3, compliance calendar + EIS deferral note in Phase 4. Next action: begin Phase 0.
- 2026-07-21/22 — **Phase 0 executed** (9 commits, `docs/spec-package`): tenant/landlord migration paths split; provisioning saga (create→migrate→seed roles/system/owner, drop-DB compensation); Asia/Manila + SINGLE_TENANT; `Money` VO + cast; Inertia v1→v2 client fix; CI (pint/larastan L6/sqlite+mariadb:11.8/frontend). Dependency pass: 31 composer + 16 npm advisories → **0**, PHP floor ^8.3, audits now blocking. Per-tenant backups per `10` §1 (`tenants:backup`, `backup_catalog` w/ tiers+legal hold, `tenants:backup-restore-test` + `restore_test_log`, nightly/monthly schedule). **Bug fixed:** hand-rolled `Tenant::makeCurrent()` skipped switch tasks — queued jobs would have hit the landlord DB; model now uses spatie `ImplementsTenant`, queue/session/cache pinned to landlord, proven by `TenantAwareQueueTest` round-trip. Spec-11 Phase-0 UI: layout fixes (Head titles, #header slot, ConfirmDialog, flash→toast bridge), kit (StatusTag/EmptyState/AppPageHeader/useAppToast/useDeleteConfirm), PrimeFlex purge in CRUD kit + Users (fixed dead search filter + 404 bulk delete). CI green (first ever — fixed withoutVite/full-pint/ziggy-vendor). **Live MariaDB 11.8 smoke test passed:** provision → backup → checksum → restore-to-scratch → verify → log → cleanup. Suite 49 passed. Remaining before Phase 1: nothing blocking; page conversions (spec 11 §4.3–7) run as warm-ups.
- 2026-07-22 (later) — **Phase 1 ledger core landed.** Tenant migrations for the full 01-spec schema (16 tables incl. partitioned hash-chained `audit_log`, tax/profile/branch tables) + T1–T5 and audit append-only triggers (MariaDB; sqlite runs app-layer only, by design 01 §2.3). `App\Domain\Ledger\`: JournalDraft/Line VOs, typed exception taxonomy, `DatabasePostingService` (idempotency-claim-before-number, accounts→period→sequence lock order, gapless draw, posting_hash, same-tx audit + balance upsert), `reverse()`/`void()` per 02 §5, `AuditLogger` (sha256 chain), `BalanceCache` (incremental upsert + roll-forward + rebuild). `ledger:verify` + `ledger:rebuild-balances`. `LedgerCoreSeeder` (types/CoA/roles map/branch/FY+13 periods/sequences with mandated prefixes/tax codes/settings) wired into provisioning. **`Ledger` testsuite** (runs last; sqlite fast path + MariaDB strict): 34 passed on real MariaDB 11.8 — trigger immutability, CHECK XOR, gapless-rollback-no-gap, idempotency, reversal-nets-to-zero, generative trial-balance-zero + cache-equals-rebuild, verify-catches-tampering. **Deviation recorded in 01 §1.2:** MariaDB err 1901 forbids CHECKs on AUTO_INCREMENT columns → no-self-parent is app-enforced. Phase 1 exit criteria: met pending CPA fixture sign-off (S1–S10 document fixtures arrive with Phase 2 document rules).
- 2026-07-22 (Phase 1 audit) — Self-audit against the Phase-1 exit list found **six gaps and one latent bug**, all now closed: (1) **BUG** `drawNumber()` filtered `document_sequences` on 2 of the 3 unique-key columns — a multi-branch tenant would have drawn an arbitrary series; now resolves the branch (01 §7). (2) period close/reopen missing → `PeriodCloseService` (close in order, drafts block the close, roll-forward, lock-date advance/retreat, audited) + `ledger:close-period`. (3) trial balance missing → `TrialBalanceService` (closed periods read the cache, open periods opening+bounded scan) + `ledger:trial-balance`. (4) opening balances → `OpeningBalanceService` (OBE absorbs the plug). (5) year-end close → `YearEndCloseService` (nominal → Income Summary → RE, routed to period 13) + `ledger:year-end-close`. (6) password rotation → `config/compliance.php` (also holds software name/version for the BIR header/footer), `password_changed_at` on both user tables, `users:password-rotation-check` + notification, `EnsurePasswordIsFresh` middleware. Also: CoA gained Equipment / **Accumulated Depreciation (contra-asset — `is_contra` was previously untested)** / Depreciation Expense; fixtures **S1, S6, S7, S8, S9 are now tests**; `ledger:verify` and the rotation check are scheduled. Suites: sqlite 88 passed, **MariaDB 11.8 95 passed (551 assertions)**. Remaining fixtures S2–S5 (documents) and S10 (inventory) belong to Phase 2/2b by design.
- 2026-07-22 (Phase 2 core) — **Documents + posting-rules layer landed.** Subledger schema (`partners` with TIN/branch/VAT-registration/sworn-declaration validity, `sales_invoices`+lines, `vendor_bills`+lines, `payments`+`payment_allocations`, `document_sequences.resets_yearly` for the never-resetting BIR invoice series per RMC 77-2024). Rules layer per 02 §2: `PostingRule` interface, `PostingContext`, `AccountResolver` (role→account from `account_roles`, loud on a missing role), `TaxResolver` (date-effective tax codes + ATC rates — rates are DATA), `RoundingPolicy` (half-up bp, exact inclusive split, Hamilton allocate), `DocumentPoster` (build → post → link `journal_entry_id` back). Three rules, both bases (D2 basis-selective): `SalesInvoicePostingRule`, `VendorBillPostingRule` (EWT accrues at booking, RR 4-2024), `PaymentPostingRule` (cash basis re-derives revenue/VAT from each allocated document's snapshot, pro-rated by largest remainder). **Fixtures S2–S5 are tests in both bases** + partial-payment pro-rating + document reversal. Suites: sqlite 95 passed, **MariaDB 11.8 102 passed (594 assertions)**. Remaining Phase 2: credit/debit memos + cancellation flow, non-VAT rules/invoice variant, invoice PDF + email, CSV import, A/R–A/P aging reports, and the document UI; advances/deposits (unapplied cash on cash basis) currently throw a clear error pending a deposit-account role.
- 2026-07-22 (Phase 2 adjustments) — **Credit/debit notes, cancellation flow, serials, aging, non-VAT and deposits landed.** (1) **Customer-facing serials are real now**: new `serial_sequences` table (per branch, CONTINUOUS — never resets, carries across a system migration per RMC 77-2024) + `DocumentSerialService` drawing under `FOR UPDATE`; `DocumentPoster` issues the number in the SAME transaction as the post, so a failed post burns none, and only when the document has none — re-posting never draws a second. This replaced the `document_sequences.resets_yearly` sentinel idea from Phase 2 core: `document_sequences.fiscal_year` is FK-bound to `fiscal_years`, so a continuous series cannot live there. Series: INV, CM, DM, RC, CV, BILL. (2) `credit_notes`(+lines) + `CreditNotePostingRule` — customer credit note debits Sales Returns & Allowances (new contra-revenue account 4100, so GROSS sales survive for the BIR columns) and reverses output VAT; vendor notes move A/P and input VAT; debit notes are the same entry with the sides swapped; cash basis posts nothing (the refund payment does). (3) **`DocumentCanceller` — the whole cancellation rule in one place:** period open → void the entry in place, mark cancelled, **retain the serial**; period closed or on/before the lock date → refuse and demand a credit note (RR 7-2024). A document with applied payment is refused outright. A reason is mandatory. (4) `DocumentBalances` recomputes `amount_paid_centavos`/status from allocations + issued notes — derived, never incremented, so a replay cannot drift; `AgingService` buckets A/R and A/P from the SUBLEDGER (current/1-30/31-60/61-90/90+), which is what lets a cash-basis registrant age receivables with no A/R control account, and ties to the A/R control account to the centavo on accrual (tested). (5) **Non-VAT (2551Q) is first-class:** `ledger_settings.is_vat_registered` + `percentage_tax_rate_bp`; a non-VAT registrant is refused if it tries to shift output VAT, and supplier input VAT is folded into cost pro-rata by largest remainder rather than parked in a claimable asset. (6) **Advances/deposits close the Phase-2-core gap** — unapplied cash is no longer an error: receipts credit Customer Deposits (2200, liability), disbursements debit Advances to Suppliers (1300, asset), and only the APPLIED portion touches the control account (previously the whole receipt hit A/R, which would drive a customer's receivable negative). Over-applying is now refused. Also: `AuditLogger` resolves the actor itself when a caller omits it (RMC 5-2021 Annex B item 8 wants a user on every row); **test-harness bug fixed** — MariaDB does not roll back AUTO_INCREMENT, so re-seeding narrow reference tables (`account_types` is a TINYINT) overflowed once the suite passed ~51 tests; `LedgerTestCase` now rewinds those counters. Suites: sqlite 113 passed, **MariaDB 11.8 120 passed (684 assertions)**; pint + larastan clean. Remaining Phase 2: BIR-compliant invoice PDF + email delivery, CSV import (partners/CoA/open documents), receipt attachments, and the document UI.
- 2026-07-22 (Phase 2 invoice document) — **BIR-faithful invoice + the compliance gate that matters more than the PDF.** `InvoiceCompliance` encodes the eleven mandatory VAT-invoice fields (NIRC Secs. 113(B) & 237 as amended; RR 7-2024 Sec. 3(B)/6) at TWO severities: the five **input-tax-fatal** omissions — amount of sales, VAT amount, registered name+TIN of both parties, description/nature of service, transaction date — **hard-block issuance** (we refuse to hand a customer a document that will fail their audit), everything else warns. Buyer name/TIN is fatal only where the law makes it mandatory (≥ ₱1,000 to a VAT-registered buyer) and a warning below, which is exactly how RR 7-2024 reads. `company_profile` is now **seeded as an obvious placeholder** whose unset TIN blocks issuance — better than printing a wrong TIN on a legal document; a missing ACCN warns (registration act, not a per-document defect). New `App\Domain\Compliance\ReportHeader` renders the header/footer block RMC 5-2021 Annex B item 4 requires on *every* generated output (registered name, address, VAT/NON-VAT TIN+branch, software name+version, generating user, Manila timestamp) — one component so a report cannot ship without it. `resources/views/documents/invoice.blade.php` is print-faithful with each mandatory field annotated to its issuance; the VAT rate is read from the date-effective `tax_codes` row, never hard-coded; non-VAT invoices print **"THIS DOCUMENT IS NOT VALID FOR CLAIM OF INPUT TAX"**, exempt/zero-rated print the mandated words, and a cancelled invoice renders CANCELLED while keeping its serial. HTML and PDF share ONE template, so what the customer sees is what gets filed. Packages: `spatie/laravel-pdf` + `spatie/browsershot` + puppeteer (production dependency — the server renders); CI's frontend-build sets `PUPPETEER_SKIP_DOWNLOAD` since it only compiles assets, and the PDF smoke test skips where Chrome is absent while the HTML assertions remain the contract. Verified end-to-end: a real two-line VAT invoice rendered through headless Chrome and read back. Suites: sqlite 124 passed, **MariaDB 11.8 131 passed (721 assertions)**; pint + larastan clean. Remaining Phase 2: email delivery of the invoice, CSV import (partners/CoA/open documents), receipt attachments via medialibrary, and the document UI.
- 2026-07-22 (Phase 2 cutover + delivery) — **CSV import and invoice email.** `CsvReader` (dependency-free `fgetcsv`, BOM-stripping — Excel writes one and it silently corrupts the first column name) + three importers, all collecting row errors rather than aborting: a cutover file with three bad rows out of four hundred must say *which three*. `PartnerImporter` upserts by code (re-running a corrected file fixes rows, never duplicates) and validates the TIN to nine digits, because a malformed TIN on an invoice is input-tax-fatal for the buyer. `ChartOfAccountsImporter` runs two passes so a child may appear above its parent, **demotes any parent to a non-postable roll-up** (posting to both a parent and its children makes the hierarchy lie), refuses self-parenting (the DB can't express it — MariaDB err 1901), and **freezes the type/normal balance of any account that already carries journal lines** rather than silently restating posted history. `OpenDocumentImporter` carries in open A/R and A/P — and the load-bearing decision: **these documents are NOT posted.** Their money is already inside the A/R/A/P figures `OpeningBalanceService` posts; posting each one too would double the receivable. New `is_opening` records *why* `journal_entry_id` is null (cash-basis and cutover both produce a null, for entirely different reasons), and `opening_paid_centavos` holds a pre-cutover part-payment separately from the `amount_paid_centavos` **cache** — a refresh recomputes from allocations and would otherwise erase history that has no allocation row to rebuild it from. `verifyAgainstOpeningBalance()` + `documents:import --verify` prove the imported detail ties to the opening balance to the centavo; the prior system's serials are preserved verbatim (RMC 77-2024). Tested end-to-end: import → post opening balance → aging total, control account and imported detail all agree. Delivery: `partners.email`, `InvoiceIssued` mailable (PDF attached, compliance gate runs inside the renderer so a defective invoice is never sent), `InvoiceMailer` **audits every send** (RMC 5-2021 Annex B item 8). Not queued yet — rendering needs both headless Chrome and the tenant connection; Phase 5 moves it to the tenant-aware queue. Suites: sqlite 136 passed, **MariaDB 11.8 143 passed (765 assertions)**; pint + larastan clean. Remaining Phase 2: receipt attachments via medialibrary, and the document UI.
- 2026-07-22 (Phase 2 UI + attachments) — **The document UI, receipt attachments, and two real bugs the work surfaced.** Controllers (`PartnerController`, `SalesInvoiceController`, `VendorBillController`, `PaymentController`, `AgingController`) validate and delegate — they never compute accounting, and never draw a serial: that stays inside `DocumentPoster`'s transaction. Ten Vue pages on the spec-11 kit (PrimeVue v4 names, `.card`, theme tokens, `AppPageHeader`/`FormSection`/`StatusTag`/`EmptyState`), plus the two components CLAUDE.md #5 mandates and the repo lacked: **`MoneyText`** (single source of peso formatting) and **`MoneyInput`** (v-model in integer centavos, `Math.round` on the way in so a float never reaches the ledger). Invoice/bill line editors recompute VAT from the tax code's **basis-point** rate with the same half-up arithmetic the posting engine uses, so the figure on screen is the figure that posts. Invoices/Show surfaces the compliance findings inline (fatal in red, warnings in amber), the produced journal entry, and cancel/email/PDF actions; Payments/Create fetches the partner's open documents and shows what will be **held as a deposit** rather than silently distorting a control account; Reports/Aging carries the mandatory BIR header block. Receipts: `VendorBill` and `Payment` are now `HasMedia` with a `receipts` collection restricted by **sniffed** mime type — the test proves a renamed executable is refused. Nothing is destroyed anywhere in this layer: invoices are cancelled, partners are deactivated. **Two bugs found and fixed:** (1) `SalesInvoiceController` ran the compliance gate before the serial was drawn, and "no serial number" is one of the eleven mandatory fields — so *no invoice could ever be issued*; `check()` now takes `requireSerial`, exempt for the pre-post gate (which must run first so a rejected invoice burns no number) and required for anything rendered or delivered. (2) **`notifications` was missing from the tenant migrations** while `HandleInertiaRequests` shares an unread count on every render — since tenant users live in the tenant DB, *every tenant page would have 500'd in production*. Also removed a duplicate tenant `media` migration I had written without checking (`0001_01_01_000200` already existed). Suites: sqlite 145 passed, **MariaDB 11.8 152 passed (809 assertions)**; pint, larastan and `npm run build` clean. **Phase 2 is complete.** Next: Phase 2b (inventory, spec `08`) or Phase 3 (reports); the CPA/tax-lawyer sign-off list in specs 03/06 still gates anything tax-final.
- 2026-07-22 (Phase 3 reporting core) — **Balance sheet, income statement, general ledger and journals — plus two serious latent bugs the reports exposed.** `App\Domain\Reports\`: `FinancialStatements` builds the balance sheet and P&L as a **partition of one trial balance** rather than from independent queries, so the Phase-3 exit criterion (everything ties to the centavo) holds *by construction* — there is no second source of truth to drift from; `tieOut()` returns that proof for the UI and the tests. The subtlety that makes a mid-year balance sheet balance: nominal accounts aren't closed until year end, so the period's net income appears in equity as its own line — and after the year-end close the same arithmetic yields zero, so the post-closing balance sheet balances with **no special case**. `GeneralLedgerReport` carries its own proof (`opening + debits − credits = closing`) and shows contra accounts per row; `JournalReport` covers all eight books from one service and **includes void entries, shown as void** — hiding them would be exactly the suppression the NIRC prohibits. **BUG 1 (trial balance, pre-existing):** `account_period_balances.opening_signed` is only authoritative once the preceding period has been rolled forward at close, but `TrialBalanceService` read it from rows created incrementally by a posting, where it is still zero. Post in January and again in July with nothing closed and **July's trial balance reported only July's movement** — the most fundamental report in the system, silently wrong. Now takes the newest *closed* period as its base and scans only the still-open tail. **BUG 2 (year-end close, pre-existing):** `YearEndCloseService` flipped every period to `closed` **without rolling balances forward**, and a closed period is read straight from `closing_signed` — so after a year-end close **every report read zero**. Now rolls forward before the flip. Also fixed a sign error in my own new code: a contra account's `signed` is positive on its own (flipped) normal side, so accumulated depreciation was *adding* to assets and sales returns would have *increased* revenue; sections now normalise onto the section's natural side. Both bugs have named regression tests. Suites: sqlite 155 passed, **MariaDB 11.8 162 passed (857 assertions)**; pint + larastan clean. Next in Phase 3: XLSX/PDF export with the mandatory header, customer SOA, manual bank reconciliation, dashboard-lite, report UI, and the Statement of Cash Flows decision (build vs explicit v2 deferral).
- 2026-07-22 (Phase 3 export + report UI) — **One description, four renderings.** `App\Domain\Reports\Export\`: a report is described once as a `ReportSheet` (title, the mandatory BIR header block, typed `ReportColumn`s, rows, totals) and comes out as **HTML, PDF, XLSX and CSV** from that single description — a format-specific report would inevitably drift from the others, and an export that disagrees with the screen is worse than no export. `SpreadsheetExporter` uses **openspout 5.3** (streaming, flat memory — a year's general ledger is not small; note openspout 5 has an immutable `Style` configured by constructor and applies styling per CELL, not per row). Money is written as a real **number** in XLSX so an accountant can sum it, and as formatted text in CSV. Every format carries the RMC 5-2021 Annex B item 4 block — registered name, address, TIN+branch, software name **and version**, generating user, Manila timestamp — and the tests assert every element of it in every format, because a book without it is not a compliant book. `ReportSheetFactory` builds sheets for trial balance, balance sheet, income statement, general ledger and all eight journals; the journal sheet is **one row per line** with the entry repeated, which is the columnar shape the BIR books use and what a spreadsheet can be filtered on. `ReportController` + `/reports/*` routes render on screen or export via `?format=`, from the same description. Five Vue pages on a shared `ReportShell` (filters, export buttons, and the header block — so a report page cannot forget it); the balance sheet and general ledger **surface their own tie-out failure as an error banner** rather than quietly printing a wrong number, and the journal shows void entries as void. Suites: sqlite 177 passed, **MariaDB 11.8 184 passed (1039 assertions)**; pint, larastan and `npm run build` clean. Remaining Phase 3: customer SOA, manual bank reconciliation, dashboard-lite, and the Statement of Cash Flows decision (build vs explicit v2 deferral).
- 2026-07-22 (Phase 3 complete) — **Customer SOA, bank reconciliation, dashboard-lite, and the cash-flow decision.** `StatementOfAccount` runs a customer's charges and credits with a brought-forward balance, from the SUBLEDGER (so a cash-basis registrant gets one too), and the page states plainly that an SOA is a **supplementary** document — proof of a running balance, not of a sale, and **not valid to support input tax** (RR 18-2012 / RMO 12-2013): a customer who files it instead of the invoice loses the claim. A cancelled invoice appears at zero rather than vanishing, because the customer may hold a copy. **Bug found here:** `whereBetween` on a `date`-cast column compares lexically against a value carrying a time component, so a statement "to today" silently dropped today's documents — now `whereDate` throughout. `BankReconciliationService` implements the classic reconciliation (statement closing + deposits in transit − outstanding cheques = book balance) with one deliberate omission: **there is no adjustment field anywhere**, because a difference is an unbooked bank item and the fix is to POST it — `complete()` refuses while a difference remains, and the UI says why. A completed reconciliation is frozen and audited. `DashboardSummary` gives cash by account, A/R and A/P split current vs overdue with the three worst payers, a P&L snapshot, and the **statutory** filing calendar (2550Q at quarter-end+25; 1601EQ; 0619E only in non-quarter-end months; 2550M is never listed — TRAIN removed it in 2023). Every figure comes from the same services the statements use, so the dashboard cannot tell a different story from the reports; the test asserts that equality directly. The Sakai demo widgets were **removed** — fabricated sales figures on an accounting dashboard are worse than an empty page. **D23 recorded: Statement of Cash Flows is DEFERRED to v2, explicitly**, with reasons (it needs per-account operating/investing/financing classification that is a CPA judgement call, plus non-cash transaction tagging the ledger does not carry; it is not a BIR filing requirement for this segment). Cash movement is fully visible through the cash-account general ledger, the reconciliation and the dashboard. Preconditions for v2 are written down, and the classification is now on the CPA sign-off list. Suites: sqlite 187 passed, **MariaDB 11.8 194 passed (1083 assertions)**; pint, larastan and `npm run build` clean. **Phase 3 is complete** — reports tie to the trial balance to the centavo and year-end close yields a correct post-closing balance sheet, both as named tests. Next: Phase 2b (inventory, spec `08`) or Phase 4 (BIR layer, CPA-gated).
- 2026-07-22 (Phase 2b slice 1 — stock ledger core) — **The inventory subledger, built to the same invariants as the journal.** Schema (08 §1): `uoms`, `locations`, `items` (with per-item GL bindings so two items can capitalise to different inventory accounts), `item_uom_conversions`, `item_barcodes`, `item_vendors`, **`stock_movements`** (append-only) and **`item_location_balances`** (derived cache). `ledger_settings` gains `grni_account_id` (D15) and `negative_stock_policy` (D17, default `block`). CoA gains Inventory 1400, **GRNI 2050** (system), COGS 5200 and Inventory Shrinkage 5300, plus the matching account roles; UoMs and one default `MAIN` location are seeded. **Append-only enforced at the DB** — triggers S1/S2/S3 mirror the journal's: an UPDATE or DELETE on `stock_movements` raises "append-only; post an opposite movement instead", and a movement against a service item or an inactive location is refused before it is written (test proves both on MariaDB). `App\Domain\Inventory\StockLedger` is THE choke point for quantity, as `PostingService` is for money: it takes the **same fixed lock order** (item → balance row) so the two subsystems cannot deadlock against each other, computes the **moving weighted average in bcmath** under a per-item `FOR UPDATE` — `(on_hand × avg + received × cost) / (on_hand + received)`, resetting to the incoming cost when on-hand is non-positive because the alternative divides by a meaningless denominator — and keeps `last_cost` strictly informational (08 §2 explicitly fixes the ninetails bifurcation where last cost drove valuation). Quantities are DECIMAL(16,5) and cost DECIMAL(19,6), but **valuation rounds per ITEM, not per location**, so rounding cannot accumulate error the GL has no matching entry for. `inventory:rebuild-balances` proves on-hand is a cache; `inventory:verify` runs three checks — cache-vs-movements, no-negative-on-hand (impossible under `block`, so its presence means something bypassed the ledger), and the **subledger-to-GL tie-out** Σ(on-hand × avg cost) vs the inventory control account, with a tolerance that defaults to ZERO. Scheduled 02:45, after `ledger:verify`, so a GL problem is reported before the tie-out that assumes the GL is sound. Suites: sqlite 199 passed, **MariaDB 11.8 207 passed (1111 assertions)**; pint + larastan clean. Next slices: goods receipts + GRNI posting rule and perpetual COGS on the sales invoice; then counts/variance, transfers; then lots/FEFO; then the receiving UI.
- 2026-07-22 (Phase 2b slice 2 — receiving, GRNI, perpetual COGS) — **The receive→sell round trip, tied to the GL.** `goods_receipts`(+lines) keep BOTH the entered quantity and its stock conversion, so a receipt reads back in the units it was typed in (08 §5); `sales_invoice_lines` gain `item_id`, `location_id` and `cogs_centavos`. `GoodsReceiptPostingRule` debits each item's OWN inventory account and credits **GRNI, not A/P** (D15) — the liability exists when the goods land, not when the paperwork does, which is exactly the gap where SMEs lose track. `CostOfSales` computes perpetual COGS at the moving average, **locking the item while it reads** so a concurrent receipt cannot move the average between the figure that lands in the journal and the figure the stock movement records. `InventoryEffects` records the stock movements in the SAME transaction as the journal entry, so there is no window where the books say goods were received and the stock ledger disagrees. **A design conflict resolved, not papered over:** spec 08 defers cash-basis COGS to collection, but the goods physically leave at shipment — deferring the *asset* movement would have broken the very subledger-to-GL tie-out that is this phase's exit criterion. Resolution: inventory falls at shipment in **both** bases, and on cash basis the cost parks in a new **Deferred COGS** system account (1450) which the payment rule releases to COGS, pro-rated, when the revenue is recognised. That preserves matching AND keeps `inventory:verify` exact; both are asserted. A cash-basis *services* invoice still posts nothing at all (fixture S3 unchanged). Selling beyond on-hand is refused and the whole post unwinds — no entry, no movement, stock intact (tested). Suites: sqlite 207 passed, **MariaDB 11.8 215 passed (1149 assertions)**; pint + larastan clean. Next: counts/variance + adjustments + transfers, then lots/FEFO, then the receiving UI.
- 2026-07-22 (Phase 2b slice 3 — counts, variance, transfers) — **The "identify variances" half of the module.** `stock_counts`(+lines), `stock_adjustments`(+lines) and `stock_transfers`(+lines). `StockCountService` runs draft → counting → review → approved, and two design choices make the count *evidence* rather than theatre: (1) **the snapshot is frozen when counting starts**, so variance measures the books against reality at one instant — without it, sales made while staff walk the aisles masquerade as shrinkage (there is a test for exactly that); (2) **blind by default**, because showing the counter what the system expects turns a count into a confirmation exercise. Variances are valued at the moving average so the review screen can rank by PESOS, not units — a 2-unit variance on an expensive item matters more than 200 on a cheap one. An uncounted line **blocks review**: it is not a zero. Approval is one-way, audited, and produces an immutable adjustment plus its journal entry; a clean count approves with no adjustment at all, which is a result rather than a failure. `StockAdjustmentPostingRule` debits shrinkage for shorts and credits it back for found stock, per ITEM account so **D16 (found stock → shrinkage contra vs Other Income, a CPA call) is a data change, not a code change**. `StockTransferService` moves quantity between locations and posts **no journal entry, deliberately** — the goods changed shelf, not value or owner, and a JE debiting and crediting the same account would be noise; both movements go in one transaction so stock is never out of one place without being in the other, and an impossible transfer fails before anything moves. Same-location transfers are refused by MariaDB's CHECK before the app guard can fire, so the test asserts the invariant rather than which layer wins. Suites: sqlite 216 passed, **MariaDB 11.8 224 passed (1182 assertions)**; pint + larastan clean. Remaining Phase 2b: lots/FEFO/expiry, then the receiving/count UI.
- 2026-07-22 (Phase 2b slice 4 — lots, FEFO, expiry) — **The prior art's best pattern, kept; its known gap, closed.** `inventory_lots` + append-only `lot_movements` (same immutability triggers as the stock ledger). Two changes spec 08 names explicitly, both implemented: **`remaining_qty` is DERIVED** (Σ `lot_movements.qty_delta`, read under `lockForUpdate()`) rather than a mutable column that can drift; and a **positive adjustment creates an "adjustment lot"** at the item's current average, closing the ninetails gap where found stock on a lot-tracked item could never be FEFO-consumed — the test proves the created lot is genuinely consumable. FEFO orders by `CASE WHEN expiry_date IS NULL THEN 1 ELSE 0 END, expiry_date, id`, so **dated stock always leaves before undated stock**, and it follows expiry rather than receipt order (tested by receiving newest-first on purpose). `release()` returns what was drawn from which lot — what a recall or a spoilage investigation actually needs. **`reverse()` is idempotent, keyed to the NET OUTSTANDING per lot for a source** — the prior art's single best idea: fire it twice (a retried job, a double-clicked void) and the second run releases nothing because the net is already zero, instead of corrupting the lot; a full reversal also reactivates a consumed lot. `require_expiry` is checked BEFORE anything persists, so a batch of receipt lines never lands half-written (08 §5). `expiringBy()` lists only lots with something left in them, so an emptied lot never clutters the expiry dashboard. Receipts open lots and sales draw them **server-side, always** — a client FEFO suggestion is a hint, never the record. `LotLedger` deliberately has NO `StockLedger` dependency: lots and quantities are two views of one event that `InventoryEffects` records in a single transaction, and coupling them would let a lot movement happen without its stock movement. Suites: sqlite 225 passed, **MariaDB 11.8 234 passed (1213 assertions)**; pint + larastan clean. Remaining Phase 2b: the receiving/count UI.
- 2026-07-22 (Phase 2b complete — inventory UI) — **Item master, receiving grid, count screens.** `ItemController` (deactivate, never delete — posted movements reference items and movements are append-only) with a valuation column read from the moving average, so the item list agrees with the balance sheet. **`avg_cost`/`last_cost` are stripped from the update payload**: costing is the stock ledger's job, and a typed average would silently restate every future COGS posting (tested). An item that requires an expiry must be lot-tracked, or there is nothing to hang the date on. `GoodsReceiptController` implements the **UoM cascade server-side** — explicit item conversion → preferred vendor pack factor → the item's own purchase factor → 1 — because a factor the browser computed is a hint, not evidence; the client shows a live preview of the same arithmetic. The **price-variance flag fires at encode time**, the cheapest moment to catch a decimal-point slip, and lot data is **batch pre-validated so a missing lot code writes nothing at all** (asserted: no receipt, no movements). The **barcode lookup is wired into the receiving grid's keyboard loop** — spec 08 §5 calls out that the prior art had the endpoint but never connected it; scanning the same code twice bumps the quantity rather than stacking duplicate lines, which is what actually happens at a delivery. `StockCountController` **withholds `snapshot_qty` from the payload entirely while a blind count is in progress** rather than hiding it in CSS, so the system quantity cannot leak to the counter; the review screen ranks variance by absolute PESO value, because two units of something expensive matter more than two hundred of something cheap. Eight Vue pages on the spec-11 kit. Suites: sqlite 236 passed, **MariaDB 11.8 245 passed (1281 assertions)**; pint, larastan and `npm run build` clean. **Phase 2b is complete** — receive→sell→count round-trips post correct JEs, `inventory:verify` ties the subledger to the GL to the centavo, count variance yields an approved immutable adjustment + JE, and FEFO is re-validated server-side. Next: Phase 4 (BIR layer) — **CPA-gated**, see the sign-off list in specs 03/06.
